import styled from "styled-components";

export const ButtonContainer = styled.button`
  background-color: transparent;
  color: #b13516;
  padding: 4px 8px;
  border-radius: 8px;
  border: solid 1px #b13516;
  font-size: 20px;
  font-weight: 600;
  cursor: pointer;
  filter: drop-shadow(0px 1px 1px rgba(0, 0, 0, 0.15));
`;
